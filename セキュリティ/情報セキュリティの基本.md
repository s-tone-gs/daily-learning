# 情報セキュリティの基本

- カテゴリ: セキュリティ
- 最終更新: 2026-06-20

---

## 当たり前の要求

情報をやり取りするとき、当たり前に求めることが3つある。

- 見るべき人にだけ見てほしい(それ以外の人には見られたくない)
- 受け取った情報の内容が正しくあってほしい(誰かに書き換えられたり、正規のものを偽って不正に再利用されたりしていない)
- 情報がちゃんと届いてほしい(必要なときに受け取れる)

これは当たり前すぎて、特に説明はいらないレベルの話。問題は、この当たり前を「情報通信」という仕組みの上でどうやって実現するか、ということ。

これを情報セキュリティの世界では「機密性・完全性・可用性」と呼ぶ(英語でConfidentiality, Integrity, Availability。略してCIA)。

| 当たり前の要求 | 呼び方 |
|---|---|
| 見るべき人にだけ見てほしい | 機密性 |
| 内容が正しくあってほしい | 完全性 |
| 情報が届いてほしい | 可用性 |

## 実現方法を考えるには、情報通信の特徴を知る必要がある

当たり前の要求が分かっただけでは、どうやって実現すればいいかは分からない。実現方法は、情報通信という仕組みがどんな特徴を持っているかによって決まる。

情報通信には、こんな特徴がある。

- 電波やネットワークを使うので、途中の情報に誰でも触れられる可能性がある(占有できない)
- データはコピーしても劣化しない。コピーと元のデータを見分けられない
- データを書き換えても、書き換えた跡が残らない
- データは0と1の組み合わせなので、数式で処理・変換できる
- 通信は機械の速さで行われるので、妨害や攻撃も機械の速さで起きる

「当たり前の要求」と「情報通信の特徴」の両方が分かって、初めて「なぜこの方法で実現しているのか」が説明できる。

この5つの特徴が「なぜそうなるのか」(記号化、なぜバイナリ表現が選ばれるか、など)を、さらに掘った前提知識(シャノン=ウィーバーモデルの「階層A:技術的な問題」にあたる)は [情報と通信の基礎](../情報工学/情報と通信の基礎.md) にまとめている。

### 3つの要求 × 5つの特徴で、やるべきことが見えてくる

3つの要求それぞれが、5つの特徴のうちどれによって妨げられるかを掛け合わせると、対応関係がはっきりする。

| | 占有できない | コピー判別不可 | 改ざん跡なし | 数式処理可能 | 機械速度 |
|---|---|---|---|---|---|
| **機密性**(見るべき人にだけ) | ⚠️アクセスを防げない | | | ✅対策の土台 | |
| **完全性**(内容が正確) | | ⚠️複製と原本の区別がつかない | ⚠️改ざんを検知できない | ✅対策の土台 | |
| **可用性**(届けたい) | ⚠️妨害が機械速度で来る | | | | ⚠️攻撃も同じ速度 |

- ⚠️は「この特徴があるせいで、要求がそのままでは満たせない」という問題点
- ✅は「この特徴があるからこそ、対策が可能になる」という解決の土台

この表から、3つの要求それぞれが5つの特徴のどれによって妨げられるかを特定し、残った「数式処理可能」という唯一の有利な性質を最大限使って、妨げを打ち消す技術(暗号化、ハッシュ・署名、冗長化)を組み立てる、という全体構造が見えてくる。

- **機密性**: 「占有できない」という問題に、「数式処理可能」を使って**暗号化**で対応する
  - 結果: 鍵を持たない人には、傍受したデータが意味のないバイト列にしか見えなくなる。「触れられる」こと自体は防げないが、「読める」ことを防げる
- **完全性**: 「コピー判別不可・改ざん跡なし」という問題に、「数式処理可能」を使って**ハッシュ・署名**で対応する
  - 結果: 内容が書き換えられていないか(改ざん)、正規のものが不正に再利用されていないか(リプレイ)を、受け取った側が検知できるようになる
- **可用性**: 「占有できない・機械速度」という問題に、**冗長化**(複数経路・複数サーバー)で対応する。可用性だけは「数式処理可能」を直接使うのではなく、構造的な対策が必要になる点が他の2つと違う
  - 結果: 1つの経路・サーバーが妨害・故障しても、別の経路・サーバーから情報を受け取れる。単一障害点(そこが壊れると全体が止まる場所)が無くなる

## 機密性:見るべき人にだけ見てほしい → 暗号化

通信は誰でも触れられる可能性がある(占有できない)。だから「触れられないようにする」ことはできない。できるのは「触れられても意味が分からない状態にする」こと。これが暗号化。

[情報と通信の基礎](../情報工学/情報と通信の基礎.md)の「階層B:意味の問題」で説明した通り、情報が伝わるには送信側と受信側が同じ「規約(符号化・復号化のルール)」を共有している必要がある。日本語のような規約は、できるだけ多くの人と共有することで、誰にでも情報が伝わるようにしている。

暗号化の「鍵」も、この規約の一種。ただし暗号化の場合は逆に、規約(鍵)を意図的に特定の人だけに共有することで、その人以外には情報が伝わらないようにする。鍵を知らない人にとって暗号化されたデータは、日本語を知らない人が日本語の音声を聞いているのと同じ状態で、ただの意味のない文字列にしか見えない。つまり暗号化とは、「情報が伝わるには規約の共有が必要」という性質を逆手に取り、規約を絞ることで伝わる相手を制限する技術だと言える。

### 「触れられないようにする」こと自体への取り組みもあった

暗号化は「触れられても読めない」状態を作る方法だが、それとは別に「そもそも触れにくくする・見つかりにくくする」という物理的なアプローチも、歴史的にいくつも試みられてきた。

- **送信電力を絞る**: 電波の届く距離は出力に比例するので、出力を必要最小限にすれば傍受できる範囲を狭められる。家庭用Wi-Fiの出力設定やBluetoothの通信距離の短さもこの考え方
- **指向性アンテナ・ビームフォーミング**: 電波を全方向ではなく特定の方向だけに絞って送る技術。狭いビームの外にはほとんど電波が届かなくなる
- **スペクトラム拡散・周波数ホッピング(FHSS)**: 送信周波数を高速で切り替えながら通信する方式。受信側は切り替えパターンを事前に知っている必要があり、パターンを知らない第三者は信号を追跡できず傍受が難しくなる。第二次世界大戦中、女優ヘディ・ラマーと作曲家ジョージ・アンタイルが、魚雷の誘導通信を敵に妨害・傍受されないようにするために発明し特許を取得した技術が元になっている。現在のBluetoothなどにも使われている
- **軍用のLPI/LPD技術**(Low Probability of Intercept/Detection): 信号をノイズに紛れ込ませるなどして、通信が行われていること自体に気づかれにくくする技術。電波版のステガノグラフィ(隠蔽)とも言える
- **電磁シールド(ファラデーケージ)・TEMPEST対策**: 機器を金属の網や箱で囲んで電波を外に漏らさない物理的対策。米軍などの「TEMPEST」規格では、画面表示などから漏れ出す微弱な電磁波まで対策の対象にしている
- **無線をやめて有線(特に光ファイバー)にする**: 光ファイバーは途中で傍受しようとすると信号の減衰が発生し検知されやすいため、有線の中でも盗聴への耐性が高いとされる

これらはいずれも、傍受される可能性を**減らす**ことはできても、**ゼロにすることはできない**。電波は物理法則に従って空間に広がるため、出力を絞っても方向を絞っても、条件が揃えば捕まえられるという前提は残る。このため実際の現場では、「物理的に捕まえにくくする(上記の技術)」と「捕まえられても読めなくする(暗号化)」を両方同時に使うのが基本になっている。Wi-Fiでも出力制限やビームフォーミングは使われているが、最終的な安全性の根拠はあくまで暗号化(WPA2/WPA3)に置かれている。

### 鍵をどう安全に渡すか

暗号化するには、事前に鍵を共有しておく必要がある。しかし、その鍵を渡す瞬間自体がまだ暗号化されていないので、そこを盗み見られたら鍵が漏れてしまう。

これを解決するのが「鍵交換」という仕組み(Diffie-Hellmanという方法など)。数学のトリックを使って、鍵そのものを一度も送らずに、お互いが同じ鍵を計算できるようにする。途中のやり取りを盗み見られても、そこから鍵自体を計算することは(現実的な時間では)できない。

### なぜ「鍵」という方法が選ばれたのか

昔は「暗号化のやり方(アルゴリズム)自体を秘密にする」方法も使われていた(シーザー暗号、エニグマなど)。しかしこの方法には弱点がある。

- やり方が一度解析されると、その仕組み全体が使えなくなる
- やり方自体を秘密にしているので、本当に安全かどうか他の人が検証できない
- 知らない相手とも安全に通信したい場合、やり方を安全に伝える手段が別に必要になる

そこで現代の暗号は、「やり方(アルゴリズム)は公開してよい。秘密にするのは"鍵"だけにする」という考え方に変わった。これを「ケルクホフスの原理」と呼ぶ。19世紀の暗号学者ケルクホフス(Kerckhoffs)が提唱したもので、現代暗号の大前提になっている考え方。(参考: Claude Shannonによる言い換え「敵はシステムを知っている」)

これにより、

- やり方を公開できるので、専門家がレビューして本当に安全かを検証できる
- 同じやり方をみんなが使えるので、メーカーが違う機器同士でも通信できる
- 秘密にする部分が鍵だけなので、漏れたら鍵を変えるだけで済む(やり方自体を作り直す必要がない)

という利点が生まれる。

### 鍵を使い捨てにする理由

接続するたびに新しい鍵を作る設計を「Forward Secrecy(前方秘匿性)」と呼ぶ。1つの鍵が将来漏れても、他のやり取りには影響しないようにするための工夫。これにより、何かが漏れたときの被害を、その1回のやり取りだけに限定できる。

## 完全性:内容が正しくあってほしい → ハッシュ・署名

データは書き換えても跡が残らないし、コピーしても元と見分けがつかない。だから「書き換えられていないこと」、そして「正規のものとして不正に再利用されていないこと」を確認する仕組みを、数式や仕組みを使って人工的に作る必要がある。

これが「ハッシュ関数」や「デジタル署名」。元のデータから特定の計算をして「指紋」のような短い値を作る。内容が少しでも変わると、その指紋も全く別の値になる。受け取った人は、自分で計算した指紋と、送られてきた指紋を比べることで「書き換えられていないか」を確認できる。

### 「コピー判別不可」が引き起こす、もう1つの問題:すり替え・再利用(リプレイ攻撃)

完全性が崩れるパターンには、実は2種類ある。

1. 内容そのものが書き換えられる(改ざん) → 「改ざんの跡が残らない」が引き起こす問題
2. 正規のデータが、別の文脈で不正に再利用される(差し替え・リプレイ) → 「コピー判別不可」が引き起こす問題

例えば、銀行への「Aさんに1万円送金してください」という正規の通信を、攻撃者がそのまま複製して保存しておき、後日もう一度送り直したらどうなるか。内容は一切改ざんされていない(元のデータと完全に同一)ので、ハッシュ・署名による「内容が変わっていないかの確認」だけでは検知できない。問題は、**この複製が「正規の、今送られた新しい指示」なのか、「過去に送られたものを悪意で再利用したもの」なのかを、受け取った側が区別できない**こと(コピーと元データが見分けつかないため)。結果、もう一度1万円が送金されてしまう。

完全性が本来確認したいのは、「受け取った内容のビット列が変わっていないか」だけでなく、「今受け取っているこの通信が、本当に意図された、正しい1回限りのやり取りか」ということ。これに対応するため、実際の通信プロトコルでは、メッセージごとに**一度しか使われない値**(ナンス、連番、タイムスタンプなど)を埋め込み、「この値は既に使われた」と検知できるようにする。これにより、内容が同一の複製であっても、「これは既に使用済みの古いコピーだ」と判定できる。

## 可用性:情報が届いてほしい → 仕組みでの冗長化

通信は機械の速さで動くので、妨害も機械の速さで起きる(大量のアクセスを送り込んで止める、など)。1つの経路や1つのサーバーだけに頼っていると、簡単に止められてしまう。

だから、複数の経路・複数のサーバーを用意しておき、1つが止まっても他で受け取れるようにする。これを「冗長化」と呼ぶ。

### 可用性は他の2つと少し違う性質を持つ

機密性・完全性は「読めるべきでない人が読めたか」「内容が変わっていたか、または正規のものが不正に再利用されていないか」という、時代に関係なく決まる基準。

可用性は違う。「見たい時に見られる」の"見たい時"は、その時代の技術でどこまで速くできるかに左右される基準。技術が「ほぼ即時にアクセスできる」を実現できるようになった結果、それが当たり前の基準になり、そこからの遅れや不通が初めて「問題」として意識されるようになった。技術が進むほど、可用性に求められる基準も上がっていく。

## まとめ

```
当たり前の要求: 見るべき人に見てほしい / 内容が正しくあってほしい / 情報が届いてほしい
  +
情報通信の特徴: 占有できない / コピーされても見分けがつかない / 書き換えの跡が残らない / 数式で扱える / 機械の速さで動く
  ↓
実現方法: 暗号化(+鍵交換、ケルクホフスの原理、Forward Secrecy) / ハッシュ・署名(+ナンス・連番による再利用防止) / 冗長化
```

Wi-Fiにおける具体的な実装(WPA、アクセスポイント、フリーWi-Fiのリスクなど)は [Wi-Fiセキュリティと暗号化の基礎](./Wi-Fiセキュリティと暗号化の基礎.md) にまとめている。
